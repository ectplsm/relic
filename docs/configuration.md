# Configuration

This guide covers Relic configuration and runtime defaults.

It will hold:

- `relic config` commands
- config precedence rules
- default Engram settings
- Claw path configuration
- memory window configuration

Top-level overview lives in [README.md](../README.md).
